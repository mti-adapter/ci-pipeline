env:
  DRY_RUN: false # set to true to disable publishing releases
  PROJECT: node-timer-source

steps:
  - name: ":hammer: :linux:"
    command: ".ci/web/tests.sh"
    artifact_paths: "dist/*"
    plugins:
      docker-compose#v3.0.0:
        config: .ci/pipeline-web-docker-compose.yml
        run: agent
